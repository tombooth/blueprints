
blueprints is a JavaScript template library for generating DOM elements.

Usage [Working]
==========================

> blueprints {directory}

will out the template file to stdout

> blueprints {directory} -o {file}

will out the template file to the specified file


Usage [Planned]
==========================

> blueprints -m {directory}

will minify the template file before outputting it

> blueprints -w {directory}

watches the template directory and updates whenever any are changed.




